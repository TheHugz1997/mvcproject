<html>
    <head>
        <meta charset="utf-8"/>
        <link rel="stylesheet" type="text/css" href="formations_list_look.css" />
    </head>
    <body>
        <h1>IT Academy</h1>
        <p>Formations disponibles</p>
        <div id="conteneur1">
            <form method="post">
                <button type="submit" name="panier" formaction="/panier">Voir le panier</button>
            </form>
            <form action="/" method="get">
                <button type="submit" name="user" formaction="/login">Se connecter</button>
            </form>
        </div>
        <div id="conteneur">
            <div class="conteneur2">
                <div class="title">Id</div>
                <% for (let i=0 ; i < lessons.length ;i++) {%>
                    <div class="lessons"> 
                        <%= lessons[i].id %>
                    </div>
                <% } %>
            </div>
            <div class="conteneur2">
                <div class="title">Nom</div>
                <% for (let i=0 ; i < lessons.length ;i++) {%>
                    <div class="lessons"> 
                        <%= lessons[i].formations_name %>
                    </div>
                <% } %>
            </div>
            <div class="conteneur2">
                <div class="title">Prix</div>
                <% for (let i=0 ; i < lessons.length ;i++) {%>
                    <div class="lessons"> 
                        <%= lessons[i].prix %>
                    </div>
                <% } %>
            </div>
            <div class="conteneur2">
                <div class="title">Debut</div>
                <% for (let i=0 ; i < lessons.length ;i++) {%>
                    <div class="lessons"> 
                        <%= lessons[i].debut %>
                    </div>
                <% } %>
            </div>
            <div class="conteneur2">
                <div class="title">Fin</div>
                <% for (let i=0 ; i < lessons.length ;i++) {%>
                    <div class="lessons"> 
                        <%= lessons[i].fin %>
                    </div>
                <% } %>
            </div>
            <div class="conteneur2">
                <div class="title"></div>
                <% for (let i=0 ; i < lessons.length ;i++) {%>
                    <div class="lessons"> 
                        <form action="/addformation" method="post">
                            <button type="submit" name="<%= lessons[i].formations_name %> ">S'inscrire</button>
                        </form>
                    </div>
                <% } %>
            </div>
        </div>
    </body>
</html>
